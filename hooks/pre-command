BUILDKITE_PATTERN="$BUILDKITE_PLUGIN_CONDITIONAL_PIPELINE_BUILDER_PATTERN"
echo "Pattern is set to ${BUILDKITE_PATTERN}"

buildkite-agent meta-data set "RUN_LINT" "true"

changed_files=$(git diff --name-only origin/main...HEAD)

echo $changed_files
